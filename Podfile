# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

target 'ABCustomUINavigationController' do

end

target 'ABCustomUINavigationControllerTests' do
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
  pod 'Specta'
  pod 'Expecta'
  pod 'OCMock'
end
